#!/usr/bin/env bash

# Get the PID of the current script
script_pid=$$

# Get the PIDs of all processes containing the word "bash"
while read -r; do
    echo "$script_pid bash"
done < <(pgrep -f bash)
